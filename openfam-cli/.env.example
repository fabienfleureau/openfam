# Open-F.A.M. CLI Configuration
# Copy this file to .env and fill in your values

# Router IP address (required)
OPENWRT_ROUTER_IP=192.168.1.1

# Router SSH port (optional, default: 22)
# OPENWRT_ROUTER_PORT=22

# SSH username (optional, default: root)
# OPENWRT_USERNAME=root

# SSH private key path (required)
# For security, only SSH key authentication is supported (no passwords)
OPENWRT_SSH_KEY_PATH=~/.ssh/id_ed25519
